# macOS / Google Drive noise
.DS_Store
._*
.Spotlight-V100
.Trashes
Icon?

# Secrets — never commit an Airtable token
.env
.env.*
!.env.example

# Node
node_modules/
npm-debug.log*


# Build outputs — regenerated from Notion by the publish workflow
roster.json
invited.json
rooms.json
assets/headshots/
